# TEST ONLY fixture keys

`test-only-private.pem` and `test-only-public.pem` are deterministic test fixtures.

**TEST ONLY — NEVER USE THESE KEYS TO SIGN PRODUCTION CONTENT.** The private key is intentionally public and provides no security. It exists only so the checked-in capsule binaries can be regenerated byte-for-byte.
